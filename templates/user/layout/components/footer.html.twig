<footer>
    <div class="footer">
        <div class="footer-menu">
            <div class="footer-logo">
                <a href="{{ path('user_homepage') }}" title="Site">
                    <img src="{{ asset('build/user/images/logo.png') }}" alt="Logo de nompaw.fr"/>
                </a>
                <span><b>Nompaw</b></span>
            </div>
            <div class="footer-items">
                <a rel="nofollow" href="{{ path('app_mentions') }}">Mentions légales</a>
                <a rel="nofollow" href="{{ path('app_politique') }}">Politique de confidentialité</a>
                <a rel="nofollow" href="{{ path('app_cookies') }}">Gestion des cookies</a>
                {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_DEVELOPER') %}
                    <a rel="nofollow" href="{{ path('admin_homepage') }}">Administration</a>
                {% endif %}
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                    <a rel="nofollow" href="{{ path('app_login') }}">Mon espace</a>
                    <a rel="nofollow" href="{{ path('app_logout') }}">Me déconnecter</a>
                {% else %}
                    <a rel="nofollow" href="{{ path('app_login') }}">Mon espace</a>
                {% endif %}
            </div>
        </div>
        <div class="copyright">Copyright 2023 - <a href="https://chanbora-chhun.fr" target="_blank" class="txt-primary">chanbora-chhun.fr</a></div>
    </div>
</footer>
