{% extends 'layout/layout_app.html.twig' %}

{% block footer %}
    <div class="relative z-10 bg-white px-4 py-8 sm:px-6 lg:px-8">
        <div class="mx-auto max-w-screen-2xl border-t pt-4 flex flex-col text-center sm:flex-row sm:text-left gap-2 justify-center">
            <p>&copy; 2024 - <a href="https://chanbora-chhun.fr" class="text-blue-600">Chanbora Chhun</a></p>
            <span>-</span>
            <a class="text-gray-700 hover:text-gray-900 hover:underline" href="{{ path('app_mentions') }}">Mentions légales</a>
            <a class="text-gray-700 hover:text-gray-900 hover:underline" href="{{ path('app_politique') }}">Politique de confidentialité</a>
            <a class="text-gray-700 hover:text-gray-900 hover:underline" href="{{ path('app_cookies') }}">Gestion des cookies</a>
            {% if is_granted('ROLE_ADMIN') %}
                <a class="text-gray-700 hover:text-gray-900 hover:underline" href="{{ path('admin_homepage') }}">Espace administrateur</a>
            {% endif %}
        </div>
    </div>
{% endblock %}
