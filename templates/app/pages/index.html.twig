{% extends 'app/layout/layout.html.twig' %}

{% block title %}Nompaw{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('app_homepage') }}
{% endblock %}

{% block main_content %}
    <div class="homepage-header">
        <div class="homepage-header-content">
            <h1>Nompaw 👋</h1>
            <div class="subtitle">
                Retrouvez pleins de recettes et un blog dédié à la cuisine !
            </div>

            <div class="cta">
                <a class="btn btn-primary" href="{{ path('app_login') }}">Accéder à mon espace</a>
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                    <a class="btn btn-outline-primary" href="{{ path('app_registration') }}">Rejoindre la communauté</a>
                    <a class="btn btn-outline-primary" href="{{ path('connect_google') }}">Se connecter avec Google</a>
                    <a class="btn btn-outline-primary" href="{{ path('connect_facebook') }}">Se connecter avec Facebook</a>
                {% endif %}
            </div>
        </div>
    </div>
    <div class="homepage-section homepage-section-0">
        {% include 'app/layout/components/deco-0.html.twig' %}
        {% include 'app/layout/components/deco-1.html.twig' %}
        <div class="homepage-section-container">
            <div class="homepage-section-title">
                <h2>Recettes ⚒️</h2>
                <div class="subtitle">Parcourez les recettes !</div>
            </div>
        </div>
    </div>
{% endblock %}
