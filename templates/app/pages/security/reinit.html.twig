{% extends 'app/pages/security/template.html.twig' %}

{% block title %}Réinitialisation de mot de passe {{ parent() }}{% endblock %}

{% block main_content %}
    <div class="homepage-header">
        <div class="homepage-header-content">
            <h1>Réinitialiser son mot de passe 🔒</h1>
        </div>
    </div>

    <div class="homepage-section homepage-section-0">
        {% include 'app/layout/components/deco-0.html.twig' %}
        {% include 'app/layout/components/deco-1.html.twig' %}
        <div class="homepage-section-container">
            {% if error is defined %}
                <div class="title-page">
                    <h1>Lien expiré</h1>
                    <p>
                        Ce lien de réinitialisation de mot de passe a expiré, veuillez recommencer la procédure. <br><br>
                    </p>
                </div>
                <div class="content">
                    <div class="exceptions-actions">
                        <a href="{{ path('app_login') }}" class="btn btn-primary">Recommencer</a>
                        <a href="{{ path('app_homepage') }}" class="btn btn-default">Revenir à la page d'accueil</a>
                    </div>
                </div>
            {% else %}
                <div id="reinit" data-token={{ token }}>
                    {% include "layout/components/loader.html.twig" %}
                </div>
            {% endif %}
        </div>
    </div>
{% endblock %}
